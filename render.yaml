services:
  - type: web
    name: aura-insights-backend
    env: docker
    dockerContext: backend
    dockerfilePath: Dockerfile
    plan: free
    region: ohio
    envVars:
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: OPENROUTER_API_KEY
        sync: false # This tells Render to ask the user for the value
      - key: RECOMMENDED_MODEL
        value: "anthropic/claude-3.5-sonnet"
    autoDeploy: true
